/* Computes the dimensional weight of a box from input provided by the user*/
 #include<stdio.h>
 int main(void)
 {
     int heigth,length,width,volume;
     heigth=10;
     length=12;
     width=15;
volume= heigth*length*width;

     printf("Volume (cubic inches):%d\n",volume);

 return 0;
 }
